"use sctrict";

const category = 'toys';

// `` - позволяют использовать интерполяцию, заместо конкотенации через +
console.log(`https://someurl.com/${category}`);

const user = "Ivan";

alert(`Привет, ${user}`);